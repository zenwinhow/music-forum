<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.springboot.mapper.ResourceCategoryMapper">
    
    <!-- 资源分类结果映射 -->
    <resultMap id="categoryResultMap" type="org.example.springboot.entity.ResourceCategory">
        <id column="category_id" property="id"/>
        <result column="category_name" property="categoryName"/>
        <result column="description" property="description"/>
        <result column="create_time" property="createTime"/>
        <result column="status" property="status"/>
    </resultMap>
    
    <!-- 分页查询资源分类 -->
    <select id="selectCategoryPage" resultMap="categoryResultMap">
        SELECT 
            *
        FROM 
            resource_category
        <where>
            <if test="categoryName != null and categoryName != ''">
                AND category_name LIKE CONCAT('%', #{categoryName}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>
    
    <!-- 获取所有有效分类（状态为1） -->
    <select id="selectAllValidCategories" resultMap="categoryResultMap">
        SELECT 
            *
        FROM 
            resource_category
        WHERE 
            status = 1
        ORDER BY 
            create_time DESC
    </select>
    
    <!-- 批量删除资源分类 -->
    <delete id="deleteByIds">
        DELETE FROM resource_category
        WHERE category_id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper> 